<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

   <!-- <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="5dp"
        android:elevation="3dp">-->

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/audio_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            >

            <ImageView
                android:id="@+id/audio_thumbnail"
                android:layout_width="0dp"
                android:layout_height="80dp"
                android:layout_margin="15dp"
                android:padding="1px"
                android:scaleType="centerCrop"
                android:adjustViewBounds="true"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintDimensionRatio="H,1:1"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toLeftOf="@id/audio_info"
                app:layout_constraintTop_toTopOf="parent"
                android:background="@color/hotpink"
                tools:ignore="MissingConstraints" />

            <LinearLayout
                android:id="@+id/audio_info"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:gravity="center"
                android:orientation="vertical"
                android:paddingLeft="10dp"
                android:layout_marginRight="15dp"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintLeft_toRightOf="@id/audio_thumbnail">

                <TextView
                    android:id="@+id/audio_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="2dp"
                    android:singleLine="true"
                    android:ellipsize="end"
                    android:textColor="@color/audio_list_title_text"
                    android:textSize="18dp"
                    tools:text="제목이 여기에 들어갑니다.들어갑니다."
                    />

                <TextView
                    android:id="@+id/audio_name"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="@color/audio_list_name_text"
                    android:textSize="13dp"
                    tools:text="작성자의 이름"
                    />
            </LinearLayout>

        </androidx.constraintlayout.widget.ConstraintLayout>
   <!-- </androidx.cardview.widget.CardView>-->
</layout>